version: '3.3'

services:
  review_autopilot:
    build: .
    container_name: review_autopilot
    restart: always
    env_file:
      - .env
    ports:
      - "8010:8010"
    volumes:
      - ./data:/app/data
    environment:
      - DATABASE_URL=sqlite:///./data/review_autopilot.db
